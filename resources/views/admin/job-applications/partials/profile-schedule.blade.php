<div class="ja-card"><div class="ja-card-title"><i class="fa fa-calendar-check-o"></i> @lang('modules.interviewSchedule.scheduleDetail')</div><div class="ja-schedule-card" style="margin-bottom:14px"><div class="ja-schedule-date">{{ $schedule->schedule_date->format('d M, Y \a\t H:i') }}</div></div>
@foreach($schedule->employee as $emp)<div class="ja-interviewer-row"><span>{{ ucwords($emp->user?->name ?? '—') }}</span><span class="ja-small-badge">{{ ucwords($emp->user_accept_status) }}</span></div>@endforeach</div>
@if($schedule->comments->isNotEmpty())<div class="ja-card"><div class="ja-card-title">@lang('modules.interviewSchedule.comments')</div>@foreach($schedule->comments as $comment)<div class="ja-note"><span class="ja-note-author">{{ $comment->user?->name ?? '—' }}</span><p class="ja-note-body">{{ $comment->comment }}</p></div>@endforeach</div>@endif
